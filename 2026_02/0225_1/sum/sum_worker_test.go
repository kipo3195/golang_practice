package sum

import (
	"testing"
)

func TestSumProcess(t *testing.T) {

	// // 항상 넣을 값과 결과값을 정의
	// input := []int{1, 2, 3, 4, 5}
	// expected := []int{2, 3, 4, 5, 6}

	// // 실제 환경과 동일하게 호출
	// w := SumWork()

	// result := SumWorkerProcess(input, w)

	// // 어떤 테스트 케이스를 가질 것인지 작성
	// if len(expected) != len(result) {
	// 	t.Errorf("예상 결과의 길이 %d와 실제 결과의 길이 %d가 다릅니다.", len(expected), len(result))
	// }

	// sort.Ints(expected)
	// sort.Ints(result)

	// for i := range result {
	// 	if expected[i] != result[i] {
	// 		t.Errorf("%d의 인덱스에서 예상 결과 %d와 실제 결과 %d가 다릅니다.", i, expected[i], result[i])
	// 	}
	// }

}
